import { Typography } from "@material-tailwind/react";

const Landing = () => {
    return (
        <div className="h-full">
            <Typography variant="h2">Landing</Typography>
        </div>
    );
};

export default Landing;
